ADMIN_GR_PASSWORD=
ADMIN_GR_SECRET=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
GRVIP_PROVIDER_API_BASE_URL=https://api.26-cdn.com/v1/api/external
GRVIP_PROVIDER_API_KEY=
# Server-side only — never expose to the client. M3U / M3U Plus playlist URL.
M3U_URL=
# Optional: HTTPS origin of an external stream relay (non-datacenter VPS) that can
# reach the IPTV host. Example: https://stream-proxy.example.com
# When set, /api/stream fetches via: $STREAM_RELAY_URL/relay?u=<encoded-source>
STREAM_RELAY_URL=
STREAM_RELAY_SECRET=
